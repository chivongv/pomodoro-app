import styled from '@emotion/styled';

export const Button = styled.button({
  padding: '1rem 1.5rem',
  marginRight: '0.5rem',
  borderRadius: 10,
  border: 'none',
  backgroundColor: '#eee',
  ':last-child': {
    margin: 0,
  },
});
